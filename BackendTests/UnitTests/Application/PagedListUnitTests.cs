namespace BackendTests.UnitTests.Application;

public class PagedListUnitTests
{
    [Fact]
    public void PagedList_Slices_Correctly()
    {
        //var items = Enumerable.Range(1, 10).Select(i => new BrandDto { Id = i, Name = $"B{i}" }).ToList();
        //var list = PagedList<BrandDto>.ToPagedList(items, count: 10, pageNumber: 2, pageSize: 3);

        //list.Metadata.PageNumber.Should().Be(2);
        //list.Metadata.PageSize.Should().Be(3);
        //list.Metadata.TotalPages.Should().Be(4);
        //list.Select(i => i.Id).Should().Equal(4, 5, 6);
    }
}
